{
  "name": "chrome-plugin-video-capture",
  "version": "1.6.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:edge": "node scripts/build-edge-package.mjs",
    "check": "npm test && node --check background.js && node --check content.js && node --check content-protocol.js && node --check offscreen.js && node --check offscreen-store.js && node --check page-recorder.js && node --check popup.js && node --check recording-coordinator.js && node --check shared.js && node --check webm-concat.js && node --check scripts/build-edge-package.mjs"
  }
}
